# ========================================
# Configuração Geral
# ========================================
TIMEZONE=America/Sao_Paulo
ROOT_DOMAIN=vps.lialean.cloud

# ========================================
# Traefik - Proxy Reverso e SSL
# ========================================
TRAEFIK_HOSTNAME=traefik.${ROOT_DOMAIN}
ACME_EMAIL=seu-email@exemplo.com

# Gerar com: htpasswd -nb admin sua_senha
# IMPORTANTE: Use aspas simples para evitar interpretação de $
TRAEFIK_BASIC_AUTH='admin:$apr1$QppNSjJe$vVT8K9XqLp2zR4mN6wS1dF'

# ========================================
# Portainer - Gerenciamento de Containers
# ========================================
PORTAINER_HOSTNAME=portainer.${ROOT_DOMAIN}

# ========================================
# PostgreSQL - Banco de Dados Principal
# ========================================
# Gerar com: openssl rand -base64 32
POSTGRES_PASSWORD=SENHA_FORTE_POSTGRES_ROOT

# ========================================
# N8N - Automação
# ========================================
N8N_HOSTNAME=n8n.${ROOT_DOMAIN}
N8N_WEBHOOK_HOSTNAME=webhook.${ROOT_DOMAIN}

# Credenciais de acesso ao N8N
N8N_BASIC_USER=admin
N8N_BASIC_PASS=SENHA_FORTE_N8N_ADMIN

# Banco de dados do N8N
N8N_DB_NAME=n8n_db
N8N_DB_USER=n8n_user
# Gerar com: openssl rand -base64 32
N8N_DB_PASSWORD=SENHA_FORTE_N8N_DB

# Chave de criptografia do N8N (32 bytes hex)
# Gerar com: openssl rand -hex 32
N8N_ENCRYPTION_KEY=CHAVE_HEX_64_CARACTERES

# ========================================
# MariaDB - Banco de Dados WordPress
# ========================================
# Gerar com: openssl rand -base64 32
MARIADB_ROOT_PASSWORD=SENHA_FORTE_MARIADB_ROOT

# ========================================
# WordPress - CMS
# ========================================
WP_HOSTNAME=wp.${ROOT_DOMAIN}

# Banco de dados do WordPress
WP_DB_NAME=wordpress
WP_DB_USER=wordpress
# Gerar com: openssl rand -base64 32
WP_DB_PASSWORD=SENHA_FORTE_WP_DB

# ========================================
# Site LiaLean - Aplicação Full-Stack
# ========================================
# Banco de dados do Site LiaLean
LIALEAN_DB_NAME=lialean_site_db
LIALEAN_DB_USER=lialean_user
# Gerar com: openssl rand -base64 32
LIALEAN_DB_PASSWORD=SENHA_FORTE_LIALEAN_DB

# JWT Secret (64 caracteres hex)
# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
LIALEAN_JWT_SECRET=CHAVE_HEX_64_CARACTERES_JWT

# Configurações do Site
LIALEAN_SITE_TITLE=LiaLean - Inteligência Artificial para o Agronegócio
LIALEAN_SITE_LOGO=/assets/logo.svg

# ========================================
# Instruções para Gerar Senhas
# ========================================
# 
# 1. Senhas gerais (32 bytes base64):
#    openssl rand -base64 32
#
# 2. Chaves hex (32 bytes = 64 caracteres):
#    openssl rand -hex 32
#
# 3. Senha do Traefik (htpasswd):
#    sudo apt install -y apache2-utils
#    htpasswd -nb admin sua_senha
#    Resultado: admin:$apr1$QppNSjJe$...
#    IMPORTANTE: Cole entre aspas simples no .env
#
# 4. JWT Secret (Node.js):
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ========================================
# ATENÇÃO: Não commitar o arquivo .env real!
# ========================================
# Este é apenas um exemplo. Copie para .env e preencha com valores reais:
# cp .env.example .env
# nano .env

